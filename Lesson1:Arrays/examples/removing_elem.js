let arr = [10, 20, 30, 40];

// Remove last
// arr.pop(); // [10,20,30]

// Remove first
// arr.shift(); // [20,30]


console.log(arr.pop()) //returns the element that got popped - arr still gets popped
console.log(arr.shift()) // returns the element that got shifted - arr still gets shifted